# Environment variables for Docker Compose
# This file eliminates warnings about unset variables

# ============================================================================
# Production Domains (required for Traefik in production, unused in local dev)
# ============================================================================
GRAFANA_DOMAIN=
SURREAL_DOMAIN=
PROMETHEUS_DOMAIN=
EXAMPLE_WEB_APP_DOMAIN=

# ============================================================================
# Service URLs (overridden in docker-compose.override.yml for local dev)
# ============================================================================

# Redis connection URL
# Local dev: redis://redis:6379 (via override)
# Production: Set to your production Redis URL
REDIS_URL=redis://redis:6379

# SurrealDB connection URL
# Local dev: ws://surrealdb:8000/rpc (via override)
# Production: Set to your production SurrealDB URL
SURREAL_URL=ws://surrealdb:8000/rpc

# PostgreSQL connection URL
# Local dev: postgresql://postgres:postgres@postgres:18100/storage (via override)
# Production: Set to your production PostgreSQL URL
DATABASE_URL=postgresql://postgres:postgres@postgres:18100/storage

# ============================================================================
# Rindexer Configuration
# ============================================================================

# RPC URL for blockchain indexing
# Set this to your Ethereum/blockchain RPC endpoint
RPC_URL=

# ============================================================================
# Redis Stream Consumer Configuration
# ============================================================================

# Consumer group suffix for Redis streams
CONSUMER_GROUP_SUFFIX=sync

# Consumer name for Redis streams
CONSUMER_NAME=main-consumer

# Analytics stream name for term updates
ANALYTICS_STREAM_NAME=term_updates
